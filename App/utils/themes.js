const color = {
    primary: '#4080ff',
    background: '#e9ebee',
    borderColor: '#eff0f1',
    link: '#0477ca'
}

const form = {
    input: {
        height: 40, 
        borderColor: color.borderColor, 
        borderWidth: 1,
        padding: 6
    }
}

const layout = {
    center: {
        alignItems: 'center',
        justifyContent: 'center'
    }
}

export { color, form, layout }
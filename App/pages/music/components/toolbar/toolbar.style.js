import { layout } from '../../../../utils/themes';

export const style = {
    container: {
        ...layout.center,
        flexDirection: 'row',
    },

    btn: {
        ...layout.center,
        width: 42,
        height: 42,
        backgroundColor: '#E0E0E0',
        borderRadius: 21,
        margin: 6
    }
}
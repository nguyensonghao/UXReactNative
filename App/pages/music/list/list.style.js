import { layout } from '../../../utils/themes';

export const style = {
    container: {
        flex: 1
    },

    footer: {
        position: 'absolute',
        left: 0,
        right: 0,
        bottom: 0
    }
}
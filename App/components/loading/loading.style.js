import { layout } from '../../utils/themes'; 

export const style = {
    container: {
        ...layout.center,
        position: 'absolute',
        left: 0,
        right: 0,
        top: 0,
        bottom: 0,
        backgroundColor: '#00000061',        
        zIndex: 1
    }
}